# Environment example for riskanaliz-site

# SMTP ayarları
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-pass
SMTP_FROM=info@yourdomain.com

# Hedef e-posta
EMAIL_TO=evrak@riskanaliz.com.tr
