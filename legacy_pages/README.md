This directory contains legacy pages that were archived to avoid conflicts with the app/ router.

If you need to restore a file, move it back to the pages/ directory or migrate the logic to the app/ router.
